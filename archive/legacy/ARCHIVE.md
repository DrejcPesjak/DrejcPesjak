# Previous profile pipeline

Archived during the AI observatory rebuild. These files are retained as reference and are not called by the active workflow. `update-readme.yml` is outside `.github/workflows`, so GitHub will not schedule it.

The archived README is an original snapshot; its relative picture links refer to the old root layout. The old raster image assets were removed during the final repository cleanup; their history remains in Git.
